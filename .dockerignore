# Dependências e artefatos reconstruídos dentro do build
node_modules
.next
out

# Client Prisma gerado — recriado por `prisma generate` no build
src/generated

# Segredos — NUNCA entram na imagem (DATABASE_URL via secret; resto via env_file)
.env
.env.*
!.env.example

# Git e ferramentas de desenvolvimento
.git
.gitignore
.husky
.vscode
.idea

# Testes e relatórios
coverage
playwright-report
test-results

# Os próprios arquivos de Docker
Dockerfile
docker-compose.yml
.dockerignore

# Logs e arquivos de SO
npm-debug.log*
*.log
.DS_Store
